============
Installation
============

At the command line::

    $ pip install tripleo-ansible

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv tripleo-ansible
    $ pip install tripleo-ansible